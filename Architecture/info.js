// A thread pool is like a team of chefs hired in advance.

// The pool has a fixed number of chefs (threads).

// New orders (tasks) go into a queue.

// Whenever a chef is free, they take the next order from the queue.

// The chefs (threads) are reused instead of being hired/fired each time.